<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.keinagroup.emefa">

    <!-- Réseaux -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Caméra -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Microphone (enregistrement audio) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Lecture de médias (Android 13+) pour accès aux fichiers audio si importés) -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Localisation (optionnelle) -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Stockage : hybride -->
    <!-- Android 12 et moins -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Notifications (si utilisées sur Android 13+) -->
    <!-- <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> -->

    <application
        android:label="Emefa"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- queries: garde uniquement si tu en as besoin -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>